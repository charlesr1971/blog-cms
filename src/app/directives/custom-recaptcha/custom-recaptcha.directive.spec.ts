import { CustomRecaptchaDirective } from './custom-recaptcha.directive';

describe('CustomRecaptchaDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomRecaptchaDirective();
    expect(directive).toBeTruthy();
  });
});
