import { ElementsOverlapDirective } from './elements-overlap.directive';

describe('ElementsOverlapDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementsOverlapDirective();
    expect(directive).toBeTruthy();
  });
});
