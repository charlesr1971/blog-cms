import { AdZoneDirective } from './ad-zone.directive';

describe('AdZoneDirective', () => {
  it('should create an instance', () => {
    const directive = new AdZoneDirective();
    expect(directive).toBeTruthy();
  });
});
