import { CharCountDirective } from './char-count.directive';

describe('CharCountDirective', () => {
  it('should create an instance', () => {
    const directive = new CharCountDirective();
    expect(directive).toBeTruthy();
  });
});
