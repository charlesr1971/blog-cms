import { TextOverflowEllipsisPipe } from './text-overflow-ellipsis.pipe';

describe('TextOverflowEllipsisPipe', () => {
  it('create an instance', () => {
    const pipe = new TextOverflowEllipsisPipe();
    expect(pipe).toBeTruthy();
  });
});
