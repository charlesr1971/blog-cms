import { ConvertIdToPathPipe } from './convert-id-to-path.pipe';

describe('ConvertIdToPathPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertIdToPathPipe();
    expect(pipe).toBeTruthy();
  });
});
