import { SeoTitleFormatPipe } from './seo-title-format.pipe';

describe('SeoTitleFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SeoTitleFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
