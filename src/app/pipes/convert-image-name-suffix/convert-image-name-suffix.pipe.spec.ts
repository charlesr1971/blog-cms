import { ConvertImageNameSuffixPipe } from './convert-image-name-suffix.pipe';

describe('ConvertImageNameSuffixPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertImageNameSuffixPipe();
    expect(pipe).toBeTruthy();
  });
});
